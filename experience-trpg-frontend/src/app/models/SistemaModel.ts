export interface Sistema {
    sistemaId: number; // ID do sistema, que é um número
    nome: string;      // Nome do sistema, que é uma string
    descricao?: string; // Descrição do sistema, opcional
}